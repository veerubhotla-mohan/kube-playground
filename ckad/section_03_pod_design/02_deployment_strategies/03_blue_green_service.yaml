apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: my-app
    slot: blue                    # change to "green" to cut traffic over to the new version
  ports:
    - port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: my-app                   # matches BOTH stable and canary pods — traffic split by replica ratio
  ports:
    - port: 80
      targetPort: 80

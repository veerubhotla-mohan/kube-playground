apiVersion: v1
kind: Pod
metadata:
  name: batch-worker
spec:
  tolerations:
    - key: dedicated
      operator: Equal
      value: batch
      effect: NoSchedule # Allow scheduling onto nodes tainted for batch work
  containers:
    - name: worker
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
apiVersion: v1
kind: Pod
metadata:
  name: env-literal
spec:
  containers:
    - name: app
      image: nginx
      env:
        - name: APP_COLOR   # literal string value
          value: blue
        - name: APP_MODE    # another literal value
          value: production

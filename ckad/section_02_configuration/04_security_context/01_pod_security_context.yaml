apiVersion: v1
kind: Pod
metadata:
  name: secure-nginx
spec:
  securityContext:
    runAsUser: 1000 # Default Linux UID for all containers in this Pod
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
